using System;
using System.Windows.Forms;

namespace CardDesignerHOT10
{
    static class Program
    {
        if (ComboBox)
            {display text Thank you
    };
        else {Display congradulations}

namespace CardDesignerHOT10
{
choose congradulations};
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            Application.Run(new Form1());
                
    
    if (ComboBox.Style) 
        { Tree if not choose party};
    if (CheckBox) 
        { checked add .25 cents};
    if stamp is checked add .50 cents;
    if custom message is checked add a Message for .30 cents add message;
            
            Cost = Style + envolope + stamp + message;
        
         }
    }
}